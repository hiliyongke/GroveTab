
## 竞品调研 + UX 缺陷修复

**竞品对比结论**（Workona / Toby / Session Buddy / OneTab / Tablerone / Tab Wrangler）：
- Canopy 的核心优势：5 种视图 + 水彩色条身份索引 + 搜索命令面板 + 去重合并
- 主要差距：无拖拽排序、无全局快捷键（manifest 缺 commands）、无标签休眠/冻结、无云同步
- 用户最看重的竞品特性：Workona 的工作区协作、Toby 的拖拽整理、Tablerone 的键盘优先命令面板

**本次修复**（4 项，tsc 0 错 / build 500ms / lint 0 error）：

1. **搜索支持 URL 搜索**
   - MiniSearch 索引新增 `url` 字段
   - 拼音 fallback 也加 `tab.url.toLowerCase().includes()`
   - 搜索提示从"标题、域名"改为"标题、域名或 URL"

2. **搜索底部状态栏 i18n 修复**
   - "navigate"/"open"/"close" 硬编码英文改为 `t('search.navigate')` / `t('search.open')` / `t('search.close')`
   - 中英文各加 3 个 i18n key

3. **归档会话重命名 UI 补齐**
   - 后端 `renameSession()` 已实现，UI 无入口
   - ArchivePanel 加 `renamingId` + `renamingValue` 状态
   - 标题区支持内联 Input 编辑（Enter 确认、Esc 取消）
   - 操作区加 EditOutlined 重命名按钮
   - i18n 加 `archive.rename` / `archive.renamePlaceholder` / `archive.renameOk`

4. **SW 上下文菜单实现 save-all**
   - 原 `canopy-save-all` 点击只 console.log
   - 现在完整实现：过滤非展示 URL / 过滤固定和隐身标签 / 保存会话到 storage / 关闭标签

5. **清理 kanban 悬空类型**
   - `UserSettings.defaultView` 移除 `'kanban'` 联合成员
   - App.tsx viewMode 加防御性校验，旧值 fallback 到 `'domain'`

6. **Popup 占位页优化**
   - 从"Quick actions coming soon..."升级为可用快捷操作页
   - 显示标签计数、打开 Canopy 主页、归档当前窗口三个入口

## 主题体系升级

**改动范围**（tsc 0 错 / build 436ms / lint 0 error 3 warning）：

1. **预设从 3 扩到 9 种**
   - 新增：default（纯白默认）/ slate（石板灰）/ warm（暖米）/ ocean（海洋）/ forest（森林）/ sunset（晚霞）/ deepspace（深空）/ midnight（午夜）/ custom（自定义占位）
   - 每个预设含 light + dark 两套渐变，运行期按 resolvedTheme 自动切换
   - 旧 aurora → slate 迁移，sunrise → warm 迁移（storage-repo.ts getSettings 自动处理）

2. **新增 `src/shared/theme/gradient-presets.ts`**
   - 集中定义所有预设的色值和元信息，SettingsPanel 和 App.tsx 共用
   - `resolveGradient(id, isDark)` 一行获取当前模式渐变 CSS
   - `compatibleMode` 标注每个预设的最佳模式（both/light/dark）

3. **设置面板渐变选择器重做**
   - 旧：3 个 64×64 Radio.Button 色块，无标签无模式指示
   - 新：3×3 grid 大色卡 + 底部名称标签 + 角标（☀/🌙 模式推荐）
   - 选中态用 primary 色 border + shadow
   - 不再使用 Radio.Group，改为原生 button + updateSettings

4. **App.tsx layoutBackground 消灭硬编码**
   - 旧：if/else 硬编码每个预设的 CSS gradient
   - 新：`resolveGradient(gradientPreset, resolvedDark)` 一行搞定

5. **lint 修复：set-state-in-effect**
   - `#search` hash 信号从 useEffect + setShowSearch 改为 useState 初始化函数
   - 符合 React 19 副作用纪律

## 竞品高优特性落地

**3 项高优竞品特性**（tsc 0 错 / build 757ms / lint 0 error 3 warning）：

1. **全局快捷键**
   - manifest.json 加 `commands` 配置：Alt+C（打开 Canopy）、Alt+Shift+S（归档当前窗口）、Alt+K（打开搜索）
   - SW 加 `chrome.commands.onCommand` 监听，open-canopy / save-all-tabs / toggle-search 三个命令
   - toggle-search 通过 URL hash `#search` 传信号，App.tsx 监听后自动聚焦搜索框
   - 设置面板加"快捷键"Tab（KeyOutlined），展示所有快捷键 + chrome://extensions/shortcuts 配置提示

2. **域名分组视图拖拽排序标签**
   - DomainGroupCard 标签列表改为 `motion/react` 的 `Reorder.Group` + `Reorder.Item`
   - 拖拽时 scale 1.02 + boxShadow 提升 + zIndex 10
   - 新增 `tabOrder` local state 管理用户手动排序
   - 合并策略同步外部变化：已有 id 保留位置，新增 id 追加末尾
   - **暂不实现分组间拖拽**——CSS multi-column 与绝对定位冲突严重

3. **标签休眠/冻结**
   - LiveTab 类型加 `discarded?: boolean` 字段
   - chrome/tabs.ts 加 `discardTab()` / `discardTabs()` 封装
   - store 加 `discardTab` / `discardDomainGroup` action
   - TabContextMenu 加"休眠标签页"菜单项（StopOutlined）
   - DomainGroupCard header 加"休眠整组"按钮
   - TabItem 休眠态显示半透明（opacity: 0.5）
   - i18n 加 tabs.discarded / tabs.discardedGroup / tabs.discardFailed / tabs.discard / tabs.discardGroup

## 产品功能全量补齐（Phase 05-12）

**8 个 Phase 全部完成**（tsc 0 错 / build 559ms）：

### Phase 05: 标签页多选与批量操作
- 新增 `selection-slice.ts`：多选状态管理（enterSelectionMode / exitSelectionMode / toggleSelect / selectAll / clearSelection）
- TabItem 支持 selectable + visibleTabIds，Ctrl/Cmd+点击多选、Shift 范围选、长按进入多选
- 新增 `BatchActionBar.tsx`：底部浮出操作栏（批量关闭/休眠/归档/取消）
- App.tsx 加 Escape 退出多选 + Ctrl/Cmd+A 全选快捷键

### Phase 06: 智能整理建议
- 新增 `idle-detect.ts`：基于 lastAccessed 计算闲置标签（stale > 7 天 / idle > 3 天）
- DedupInfoBar 升级为 `TidySuggestionBar.tsx`：同时展示重复 + 闲置，支持一键整理
- 一键整理 = 合并重复 + 休眠闲置，undo 系统兜底

### Phase 07: Chrome 原生 Tab Groups 集成
- manifest 加 `tabGroups` permission
- chrome/tabs.ts 加 `queryTabGroups` / `updateTabGroup` / `groupTabs` / `ungroupTabs` 封装
- LiveTab 加 `groupTitle` / `groupColor` 字段
- loadAllTabs 同时拉取 Tab Group 信息并注入
- 新增 `TabGroupView.tsx`：按 Chrome 原生分组展示，未分组标签单独一区

### Phase 08: 多窗口管理面板
- chrome/tabs.ts 加 `moveTab` / `moveTabs` 跨窗口移动 API
- 新增 `WindowView.tsx`：按窗口分组，当前窗口置顶，支持"合并到当前窗口"
- 合并逻辑：逐批移动其他窗口标签到当前窗口（每批 10 个防超时）

### Phase 09: 标签页预览缩略图
- manifest 加 `activeTab` permission
- 新增 `TabPreview.tsx`：hover 弹出截屏预览（chrome.tabs.captureVisibleTab）
- 预览缓存到 chrome.storage.session（MV3 专用，浏览器关闭自动清除）
- UserSettings 加 `tabPreviewEnabled` 设置项

### Phase 10: 书签集成
- manifest optional_permissions 加 `bookmarks`
- 新增 `chrome/bookmarks.ts`：queryBookmarks / requestBookmarkPermission / bookmarkTabs 封装
- 新增 `BookmarkView.tsx`：权限请求 → 树形浏览 + 搜索 → 一键收藏全部标签
- antd Tree 组件展示书签层级，Input 搜索支持 MiniSearch 模糊匹配

### Phase 11: 标签页分屏
- chrome/tabs.ts 加 `splitTabToSide(tabId)`：将标签移到新窗口并 50/50 分屏
- TabContextMenu 加"分屏显示"菜单项（ColumnWidthOutlined 图标）
- 流程：获取原窗口位置 → chrome.windows.create 移标签 → 原窗口缩左半 → 新窗口缩右半

### Phase 12: 自定义渐变编辑器
- gradient-presets.ts 加 `buildGradient(stops, angle)` 导出函数
- resolveGradient 支持 customGradient 参数消费用户自定义渐变
- SettingsPanel custom 卡片点击后展开渐变编辑器：
  - 角度滑块（0°-360°）
  - 2-6 个色标，每个含 ColorPicker + position 滑块
  - 实时预览条
  - 深色模式独立配色（开关 → 自动暗化生成 + 独立编辑）
  - 加/删色标按钮

## Phase 05-12 全量功能补齐（续）

**本次完成**：
- Phase 11 标签页分屏：已有 splitTabToSide API + 右键菜单集成，确认完整
- Phase 12 自定义渐变编辑器：已有 ColorPicker+Slider 色标编辑、角度调节、深色模式独立配色、实时预览
- 设置面板 defaultView 下拉更新为 8 个视图选项
- resolveGradient 支持 customGradient 参数，custom 预设色卡实时预览
- 修复 TidySuggestionBar rules-of-hooks（early return 移到 hooks 之后）
- 修复 TabPreview set-state-in-effect（改用 onOpenChange 事件回调替代 useEffect）
- 删除多余的独立 GradientEditor.tsx（编辑器已内嵌在 SettingsPanel）

**最终构建状态**：tsc 0 error / vite build 485ms / eslint 0 error 4 warning

## 新增预览图方格视图（PreviewGridView）

**用户诉求**：增加一种窗口模式的预览图模式，用方格子把所有 tab 预览图列举出来。

**实现要点**：
- 新增 `src/features/tabs/PreviewGridView.tsx`：每个标签页一张方格卡片，CSS Grid 响应式列数（auto-fill, minmax(220px, 1fr)）
- 预览图区 16:10 宽高比，优先展示截屏预览；无预览时降级为域名色渐变 + favicon/首字母
- 挂载后自动截取当前窗口活跃标签的预览图（复用 captureVisibleTab 策略），非活跃标签显示降级 UI
- 卡片底部显示 favicon + 标题 + 域名，hover 时显示关闭按钮（红色）和音频指示
- 左下角 2px 域名色条（hover 3px），与 DomainGroupCard 视觉纪律一致
- 批次内 Accent 去重分配（useGroupAccents），避免并排卡片撞色

**注册改动**：
- types.ts defaultView 联合类型加 `'preview'`
- App.tsx ViewMode / validViews / Segmented options / 条件渲染 / import 全部更新
- features/tabs/index.ts 导出新组件
- SettingsPanel.tsx defaultView Select 加 preview 选项
- i18n zh-CN/en 加 `view.preview` / `view.previewCapturing`

**构建状态**：tsc 0 error / vite build 769ms / eslint 0 error 3 warning（均为既有问题）

## 预览截图功能下线

**决策原因**：`chrome.tabs.captureVisibleTab` 只能截取当前窗口的"当前活跃标签页"，无法跨标签截屏。hover 预览弹窗和 PreviewGridView 方格视图本质上都依赖这个功能，但实际效果与预期不符，用户决定整体下线。

**清理范围**：
1. **删除组件**：`TabPreview.tsx`、`PreviewGridView.tsx`
2. **移除包裹**：6 个视图中的 TabPreview 包裹全部移除
   - `TimelineView`、`DomainGroupCard`、`FrequencyView`、`TabGroupView`、`WindowView`、`GridView`
3. **移除注册**：`index.ts` export、`App.tsx` preview 视图分支、`SettingsPanel` tabPreviewEnabled 开关 + preview 选项
4. **清理类型**：`UserSettings` 移除 `tabPreviewEnabled` 字段和 `preview` 视图值
5. **清理 i18n**：zh-CN/en 移除 `settings.tabPreview`、`view.preview*`、`preview.unavailable` 等 7 条文案

**构建验证**：tsc 0 error / vite build 717ms ✅

## 架构治理（P0→P2）

**按建议优先级执行优化**，tsc 0 error / build 572ms / lint 0 error：

### P0-1: DomainGroupCard 修复 React 19 渲染期 setState
- `tabOrder` state（完整对象数组）→ `orderOverride`（ID 数组）+ `useMemo` 派生
- 拖拽时只存 ID 顺序，group.tabs 变化时 useMemo 自动合并新增/过滤删除
- 消除渲染期直接 `setTabOrder()` 的 React 19 违规

### P0-2: SettingsPanel 拆分（880 行 → ~40 行外壳 + 5 个子文件）
- `panels/AppearancePanel.tsx`：语言 + 渐变预设网格 + 自定义渐变编辑器
- `panels/BehaviorPanel.tsx`：默认视图 + 列数 + favicon + 色条 + 圆角 + 时间轴
- `panels/DataPanel.tsx`：存储配额 + 导出/导入/清空（完全自包含状态）
- `panels/ShortcutsPanel.tsx`：快捷键列表
- `components/Field.tsx`：label + control + hint 共享包装

### P0-3: ArchivePanel 拆分（511 行 → ~180 行外壳 + 独立组件）
- `components/SessionItem.tsx`：主行（标题/展开/操作）+ 展开详情（tab 列表）+ 重命名态
- ArchivePanel 只保留 Modal 外壳、外部 store 同步、操作回调

### P2-1: 视图配置集中到 views.ts
- 新建 `src/shared/config/views.ts`：VIEW_CONFIGS / VALID_VIEWS / VIEW_MAP
- App.tsx HeroBar Segmented 和 BehaviorPanel defaultView Select 统一从配置消费
- 新增视图只需改一处，不再分散在 4 个地方

### 跳过的项（收益低或引入过度抽象）
- P1（提取 service + 解耦 undo）：store 层内部耦合合理，强行解耦引入全局回调
- P2-2（提取共享 DomainCard）：GridCard 与 DomainGroupCard 结构差异大，抽象收益低

## ESLint 5星达标（2026-04-23）

**43 个 ESLint type-aware errors → 0 全部修复。**

修复模式总结：
- `no-misused-promises`（async onClick/handler）：`onEvent={asyncFn}` → `onEvent={() => { void asyncFn(); }}`
- `no-misused-promises`（async IIFE）：`onClick={async () => {...}}` → `onClick={() => { void (async () => {...})(); }}`
- `no-floating-promises`：加 `void` 前缀
- `tabs-slice.ts handleBroadcast`：静默刷新 `void get().loadAllTabs({ silent: true })`
- `sw/index.ts`：`addListener` 的 async 回调 → IIFE 包装
- `undo-slice.ts addRecord`：`return record` → `return Promise.resolve(record)`（接口要求 `Promise<UndoRecord>`）
- `vite.config.ts`：`Manifest` 接口中 `action`/`icons` 改为 required，避免每次都要 `!`

剩余 178 个 warnings 均为 `strict-boolean-expressions` / `prefer-nullish-coalescing`（低优先级类型风格警告，不影响 build/lint 评级）。

## 修复：休眠 tab 后其他 Canopy 窗口未同步（2026-04-23）

**问题根因**：
1. `discardTab` / `discardDomainGroup` 调 `loadAllTabs({ silent: true })` 只刷新了当前窗口
2. SW 没有监听 `tab-discarded` 广播事件
3. `handleBroadcast` 没有 `tab-discarded` case

**修复方案**：
1. `src/shared/types.ts`：SwBroadcastType 增加 `tab-discarded`
2. `src/shared/utils/sw-broadcast.ts`：**新建**，统一 BroadcastChannel 封装（SW 和 store 共用）
3. `src/sw/index.ts`：
   - 用 `swBroadcast` 替换所有 `broadcast` 调用
   - `onUpdated` 增加 discarded 状态比对检测
   - `onAlarm`（每分钟）轮询比对所有 tab 的 discarded 状态作为兜底
4. `src/store/tabs-slice.ts`：
   - `discardTab` / `discardDomainGroup` 之后立即调用 `swBroadcast('tab-discarded', ...)` 通知所有窗口
   - `handleBroadcast` 增加 `tab-discarded` case → 静默全量刷新

## 依赖清理 + 死代码移除（2026-04-23）

**触发**：用户要求清理无用依赖、死代码，更新所有依赖到最新稳定版。

### 依赖变更（package.json）

**移除**（未在 src/ 中引用）：
- `@ant-design/colors`（antd v6 不再需要单独引入）
- `class-variance-authority`（已全面迁移到 antd）
- `@testing-library/jest-dom`（无测试用例引用）
- `@testing-library/react`（无测试用例引用）
- `@typescript-eslint/eslint-plugin`（已迁移到 `typescript-eslint` 统一包）
- `@typescript-eslint/parser`（同上）

**更新到最新稳定版**：
| 包 | 旧版 | 新版 |
|---|---|---|
| `antd` | ^6.0.0 | ^6.3.6 |
| `@ant-design/icons` | 6 | ^6.1.1 |
| `react` / `react-dom` | ^19.2.0 | ^19.2.5 |
| `@types/react` | ^19.2.0 | ^19.2.14 |
| `@types/react-dom` | ^19.2.0 | ^19.2.3 |
| `vite` | ^8.0.0 | ^8.0.10 |
| `@vitejs/plugin-react` | ^6.0.0 | ^6.0.1 |
| `eslint` | ^10.2.0 | ^10.2.1 |
| `eslint-plugin-react-hooks` | ^7.1.0 | ^7.1.1 |
| `jsdom` | ^29.0.0 | ^29.0.2 |
| `prettier` | ^3.8.0 | ^3.8.3 |
| `typescript` | ^6.0.0 | ^6.0.3 |
| `vitest` | ^4.1.0 | ^4.1.5 |

### 死代码移除

**文件删除**：
- `src/features/tabs/DedupInfoBar.tsx`（无任何引用）

**导出清理（保留函数体，仅移除 export 或删除未使用导出）**：
- `src/features/tabs/index.ts`：移除 `TabItem`、`DedupInfoBar` 的 re-export
- `src/chrome/tabs.ts`：移除 `queryCurrentWindowTabs`、`getTab`、`getWindow`、`createWindowWithTabs`、`getRecentlyClosed`、`restoreSession`、`updateTabGroup`、`groupTabs`、`ungroupTabs`、`moveTab`
- `src/chrome/bookmarks.ts`：移除 `getRecentBookmarks`
- `src/chrome/utils.ts`：`classifyUrl`、`isCanopyNewTab` 改为未导出（内部使用）
- `src/repositories/storage-repo.ts`：移除 `removeData`、`getStorageUsage`
- `src/shared/hooks/index.ts`：移除 `useAccent` re-export
- `src/shared/i18n/core.ts`：`dictionaries`、`formatDate`、`formatNumber` 改为未导出
- `src/shared/utils/color.ts`：移除 `normalizeColorKey`、`stringToDopamineHue`、`stringToHue`
- `src/shared/utils/dedupe.ts`：`normalizeUrl` 改为未导出
- `src/shared/utils/domain.ts`：移除 `getDomainInfo`、`getGroupDomain`、`getRegisteredDomain`
- `src/shared/utils/metrics.ts`：`getMetrics` 改为未导出
- `src/shared/utils/pinyin.ts`：`getPinyinInitials` 改为未导出
- `src/shared/utils/url-display.ts`：移除 `isDifferentPage`
- `src/shared/config/views.ts`：移除 `VIEW_MAP` 导出

### 验证
- tsc：0 error ✅
- build：531ms ✅
- lint：0 error / 175 warnings（均为已有警告，无新增）✅
