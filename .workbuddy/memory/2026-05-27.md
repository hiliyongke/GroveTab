# 2026-05-27 GroveTab 开发日志

## 任务列表

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 5 | 搜索索引：切换到 Web Worker | ✅ 完成 | 新建 use-search-bridge.ts，SearchBox 改用 Worker 索引 |
| 6 | 全局 Cmd/Ctrl+K 快捷键 | ✅ 完成 | SW 改为查找窗口聚焦+消息，manifest 增加 mac 支持 |
| 7 | ArchiveView 重构：接入 Zustand sessions-slice | ✅ 完成 | 新建 sessions-slice.ts，ArchiveView 全部改用 Zustand 订阅 |
| 8 | DomainGroupView 虚拟滚动 | ✅ 完成 | 引入 @tanstack/react-virtual，超过 20 分组启用 |

## 代码改动

### 1. 搜索索引修复
- 新增：`src/features/search/hooks/use-search-bridge.ts`
- 修改：`src/features/search/SearchBox.tsx`（import 切换 + 调用顺序调整）

### 2. Cmd/Ctrl+K 快捷键
- 修改：`src/sw/index.ts`（toggle-search 逻辑重写）
- 修改：`src/pages/newtab/App.tsx`（新增 toggle-search 消息监听）
- 修改：`manifest.json`（增加 mac: Command+K）

### 3. ArchiveView 重构
- 新增：`src/store/sessions-slice.ts`
- 修改：`src/store/index.ts`（新增 useSessionsStore 导出）
- 修改：`src/features/sessions/ArchiveView.tsx`（useSyncExternalStore → useSessionsStore）

### 4. DomainGroupView 虚拟滚动
- 重写：`src/features/tabs/DomainGroupView.tsx`（引入 VirtualColumn 组件 + useVirtualizer）

## 构建验证

- TypeScript 类型检查：通过
- 单元测试：16 个文件，183 个测试，全部通过
- Vite 构建：通过（newtab.js 809KB）

## 产品能力缺陷分析

已写入：`deliverables/product-strategy/grovetab-product-gap-analysis-2026-05-27.md`

### 四个核心短板（P0 优先级）
1. **跨设备同步** — 完全缺失，chrome.storage.local 无法支撑
2. **数据导入/导出** — 仅 JSON，需增强 HTML/CSV 格式
3. **首次使用引导** — onboarding 过于简单
4. **存储分层** — 大会话数据易触发 10MB 上限

### P2 差异化方向
- AI 智能分组（竞品均未成熟，GroveTab 可探索）
- 偷瞄模式是唯一差异化亮点（竞品无一具备）

## 备注

- 所有改动均在 /Users/yorke/Desktop/tabs/ 目录下
- 每次 commit 建议单独进行，验证通过后再进行下一个

---

## 能力补齐（单机应用视角）

### 调研发现

经过详细代码调研，很多"缺陷"其实已有完整实现：
- ✅ 批量操作 — BatchActionBar 已完整
- ✅ 快捷键自定义 — ShortcutsPanel 已有录制+冲突检测
- ✅ 暗色主题 — useResolvedTheme + 深色渐变/皮肤
- ✅ 虚拟滚动 + 排序 — DomainGroupView 已有
- ✅ 多格式导入导出 — JSON/HTML/OneTab/Markdown/TXT
- ✅ 搜索高亮 — SearchHighlight 组件已存在
- ✅ 首次引导 — OnboardingCard v2 已完整
- ✅ 存储配额可视化 — DataPanel 已有
- ✅ 配置预设 — DataPanel 支持多预设
- ✅ 内存治理 — MemoryGovernanceSettings 已有

### 真正缺失的 3 项能力（已补齐）

| # | 能力 | 状态 | 关键文件 |
|---|------|------|----------|
| 1 | **回收站/垃圾箱** | ✅ | TrashView.tsx, trash-repo.ts, tabs-slice.ts |
| 2 | **DomainGroupView 搜索过滤** | ✅ | DomainGroupView.tsx（新增 Input 过滤） |
| 3 | **存储自动清理策略** | ✅ | use-auto-cleanup.ts（触发阈值 80%） |

### 回收站设计
- 关闭标签页时自动放入回收站（fire-and-forget，不阻塞关闭）
- 7 天自动过期清理
- 支持恢复（在新标签页打开）、删除、清空
- 最多保留 200 条记录
- 入口在顶栏右侧（历史记录按钮旁边）

### DomainGroupView 搜索过滤
- 在分组区域上方添加搜索输入框
- 按域名/标签标题实时过滤
- 空状态提示

### 存储自动清理策略
- 页面加载 3 秒后检测存储使用率
- 超过 80% 时自动清理（优先级：metrics → activity → historyEvents → searchHistory → trash）
- 清理后通过 feedback.info 通知用户

### 构建验证
- TypeScript 类型检查：0 错误
- 单元测试：16 文件 183 测试，全部通过
- Commit：`feat: 回收站 + DomainGroupView搜索过滤 + 存储自动清理策略`

---

## 产品功能架构审视（2026-05-27）

### 核心发现

1. **视图模式过度设计**：10 种视图模式（domain/compact/timeline/tabgroup/window/kanban/bookmarks/frequency/grid/archive），竞品仅 1-3 种
2. **目录分类混乱**：BookmarkTreeView/BookmarkView 放在 `features/tabs/` 下，与标签管理混淆
3. **页面模式偏离核心**：DeveloperToolsPage 和 TrendingPage 作为一级页面模式，与「标签管理器」定位偏离
4. **设置面板可合并**：BehaviorPanel 已内聚 5 个子面板，一级 Tab 可从 6 个合并为 4 个
5. **历史相关功能分散**：HistoryPanel/TimelineView/ArchiveView/TrashView 都与「过去」相关，但入口分散

### 建议整合方向

- 视图模式：10 → 5 核心（domain/window/timeline/tabgroup + 2 布局选项）
- 设置面板：6 Tab → 4 Tab（appearance/behavior/system/about）
- 书签视图：从 `features/tabs/` 移至 `features/bookmarks/views/`
- archive/bookmarks：从 ViewDock 移出，改为独立入口
- devtools/trending：从 page mode 降级为可选模块

### 完整报告
- 文件：`deliverables/product-strategy/feature-architecture-review-2026-05-27.md`