## GroveTab 当前项目产品体验审查报告

- **审查日期**：2026-05-21
- **审查对象**：`GroveTab / 林栖标签页`
- **审查方式**：基于当前代码、现有文档、配置文件、构建结果、测试结果的静态审查
- **说明**：本报告以**当前仓库可确认的实现状态**为准，不把历史 PRD、旧版 Changelog、旧分析报告中的能力默认视为“已经真实交付给用户”

---

## 一、执行摘要

这是一个**完成度很高、设计野心很强、功能密度很高**的新标签页扩展项目。它的核心长板很清楚：**本地优先、搜索与整理能力强、主题系统成熟、工程分层整体健康**。但它当前最大的短板也很清楚：**产品边界不够收束，品牌与文档不一致，部分叙事与当前实际界面存在偏差，导致用户会感到“能力很多，但主线不够聚焦”**。

如果用一句话概括当前状态：

> **这不是一个做不出来的产品，而是一个已经做出很多东西，但需要重新收束产品叙事和首要体验的产品。**

从产品判断看，当前项目最适合被定义为：

> **以标签页整理和会话归档为核心的本地工作台扩展，附带热榜浏览和开发者工具能力。**

而不是：

> “一个把任何新标签页需求都塞进来的全能首页。”

这两种定义的差别很大。当前实现更接近前者，但界面结构和文档表达经常滑向后者。

---

## 二、审查方法与证据来源

本次结论主要来自以下几类信息：

### 1. 当前主入口与核心页面

- `src/pages/newtab/App.tsx`
- `src/features/search/SearchBox.tsx`
- `src/features/settings/SettingsPanel.tsx`
- `src/features/settings/panels/AppearancePanel.tsx`
- `src/features/settings/panels/BehaviorPanel.tsx`
- `src/features/settings/panels/DataPanel.tsx`
- `src/features/settings/panels/AboutPanel.tsx`
- `src/pages/popup/App.tsx`
- `src/features/trending/TrendingPage.tsx`
- `src/features/developer-tools/DeveloperToolsPage.tsx`

### 2. 产品与架构文档

- `README.md`
- `ARCHITECTURE.md`
- `CHANGELOG.md`
- `QA_CHECKLIST.md`
- `PRODUCT-ANALYSIS-REPORT.md`
- `docs/PRD v1.0.md`
- `PRIVACY.md`

### 3. 主题、品牌与配置系统

- `src/shared/theme/skin-presets.ts`
- `src/shared/ui/AntdThemeProvider.tsx`
- `src/shared/config/brand.js`
- `manifest.json`
- `package.json`

### 4. 工程验证结果

- **测试**：`pnpm test` 通过，**13 个测试文件 / 169 个测试全部通过**
- **构建**：`pnpm build` 通过，生产包可正常生成
- **Lint**：`pnpm lint` 未能完成，原因不是规则报错，而是 `eslint.config.js` 依赖的 `@eslint/js` 缺失，属于**工具链依赖不完整**

因此，本报告既包含产品体验判断，也包含“工程状态如何影响体验可信度”的判断。

---

## 三、当前产品的真实定位判断

基于当前代码，产品目前实际上是一个三层结构：

### 1. 主价值层：标签工作台

这是产品的真正核心，包含：

- 标签页聚合与多视图浏览
- 搜索与快速切换
- 会话归档与恢复
- 去重、闲置识别、批量操作
- 工作区过滤
- 书签 / 历史 / 看板 / 归档视图

这是**最强、最完整、最有差异化**的一层。

### 2. 扩展价值层：热榜页

新标签页支持切换到 `trending` 模式，提供多平台热榜聚合、分类切换、偷摸模式。它更像一个“顺手会用”的附加能力，而不是产品核心。

### 3. 平行能力层：开发工具栏

新标签页还支持切换到 `devtools` 模式，内置大量本地开发小工具。这部分本身做得不差，但与“标签页管理工作台”的核心心智**不是同一类问题**。

### 结论

当前产品的结构不是“一个单一任务产品”，而是“**一个强主业 + 两个副业**”的产品。

这并不一定错，但会带来一个直接问题：

- 对老用户来说，功能丰富
- 对新用户来说，产品一句话很难说清

也就是说，**能力越多，定位越需要克制**。当前项目的主要问题不是能力不够，而是**能力之间缺少层级管理**。

---

## 四、当前已确认的核心体验链路

### 1. 新用户首次进入

当前首页首次进入会展示 `OnboardingCard`：

- 让用户二选一：
  - 接管新标签页
  - 仅工具栏按钮
- 若选择接管，再进入 3 步微引导：
  - 工作台总览
  - 归档与恢复
  - 快捷键帮助

### 2. 主界面体验

新标签页主界面的稳定骨架是：

- 顶部 `Header`
- 中部 `HeroBar`
- 搜索入口
- 视图切换
- 整理建议栏
- 主视图区域
- 右侧设置 / 洞察 / 搜索浮层等二级面板

### 3. 二级空间切换

Header 上直接提供一级切换：

- `workspace`
- `trending`
- `devtools`

这使得用户可以在同一个入口下切到完全不同的任务场景。

### 4. 弹窗轻量入口

Popup 提供独立的轻量工作流：

- 搜索当前标签
- 查看最近标签
- 一键关闭某个标签
- 归档当前窗口
- 打开完整工作台

这是一条非常实用的辅助链路。

---

## 五、产品体验的主要优点

## 5.1 价值主张真实，不是空喊口号

项目最可贵的一点是：**“本地优先、隐私优先、标签整理优先”并不是营销话术，而是代码层真实成立的。**

具体表现：

- 主要数据保存在本地 `chrome.storage.local` 和 `IndexedDB`
- 提供存储配额可视化
- 提供导入 / 导出 / 清除 / 全量重置
- 可选权限采用按需申请
- 搜索增强的 `OG` 抓取是显式开关，不是默认开启
- 本地洞察明确标注“不会上传”

这会直接带来一种很强的用户信任感：

- 用户知道数据在哪
- 用户知道权限何时申请
- 用户知道如何撤销
- 用户知道可以备份和迁移

对于标签管理工具来说，这种信任非常重要。

---

## 5.2 主任务链路有明显效率价值

工作台主链路的价值是成立的：

- 搜索入口明确，`Cmd/Ctrl + K` 心智强
- 多视图切换对重度标签用户有意义
- 去重与闲置检测解决真实痛点
- 会话归档 / 恢复闭环清晰
- Undo 机制降低误操作风险
- Popup 让高频动作不用每次打开完整新标签页

这说明它不是“好看但没用”的首页，而是一个**能显著改善浏览器信息整理效率**的工具。

---

## 5.3 视觉系统成熟，且不是单一皮肤糊上去

这个项目的主题系统是明显下过功夫的。

不是简单的浅色 / 深色切换，而是完整的视觉 token 系统：

- **9 套皮肤预设**
- 皮肤定义了字体、圆角、阴影、模糊、背景、搜索框、Header、浮层等完整参数
- `AntdThemeProvider` 会把皮肤转成 antd 主题和自定义 CSS 变量
- 支持极客模式逐项覆盖 token

这带来的体验优势有三点：

- 视觉一致性比一般扩展高很多
- 个性化能力足够深，不是“换个背景色”那种浅层定制
- 设计资产沉淀成系统，后续扩展和复用成本低

这是项目非常明显的长板。

---

## 5.4 Popup 做得比很多同类产品更实用

很多扩展的 Popup 只是“一个打开主页面的按钮集合”。当前 Popup 明显更像真实的工作入口：

- 直接显示最近标签
- 支持快速聚焦和关闭
- 回车直接走网页搜索
- 支持一键归档当前窗口
- 一键打开工作台

这条链路很适合：

- 不想打断当前浏览节奏的用户
- 只想做一个小动作的用户
- 习惯从工具栏解决问题的用户

这会提升产品“常驻性”，是留存点之一。

---

## 5.5 开发工具栏本身质量不低

虽然我后面会讲它与主产品定位之间的冲突，但单看实现，这一页并不敷衍：

- 工具数量多，覆盖面广
- 分类、搜索、收藏、最近使用都有
- 输入输出区和双向转换逻辑完整
- 大量工具支持实时执行
- 明确写了“纯本地处理”
- 本地存储收藏和最近使用，体验闭环完整

如果把它当成一个独立小产品，它是有可用性的。

---

## 5.6 工程基础整体健康

从产品体验角度，工程质量决定“能不能长期好用”。这一点上，本项目比普通个人扩展要强。

### 可确认的正向信号

- `pnpm test` 全绿，169 个测试通过
- `pnpm build` 成功
- 模块划分基本清楚：UI / Store / Service / Repository / Chrome 封装
- 许多非首屏能力做了懒加载
- 归档存储支持 `IndexedDB` 降级
- Undo、搜索、视图注册、权限控制都有单独抽象

这意味着：

- 产品不是“只能演示”的状态
- 有继续演进的基础
- 核心逻辑的可回归性较好

---

## 六、当前产品体验的主要问题

## 6.1 最大问题：产品边界不够收束

当前新标签页入口同时承载：

- 标签工作台
- 全网热榜
- 开发工具栏

这三者分别解决的是三种不同的问题：

- 标签管理
- 信息消费
- 开发效率

它们不是不能放在一起，但目前缺少一个清晰的层级关系。结果是：

- **产品很丰富**，但一句话不容易说清
- **首页功能很多**，但新用户不知道先做什么
- **每个分支都成立**，但彼此会争抢入口优先级

从体验上看，这会带来认知负担：

- 我装这个扩展到底是为了整理标签，还是看热榜，还是做开发工具？
- 新标签页打开后最该突出哪个能力？
- 哪个才是产品的“默认心智”？

当前代码层面的默认值是 `workspace`，这说明项目自己其实也知道主产品是工作台。但 Header 又把 `trending` 和 `devtools` 放在了同级一级入口，削弱了这个判断。

### 结论

**这是一个主产品做得很强，但副产品入口过重的结构问题。**

---

## 6.2 文档、版本叙事与当前实际界面存在偏差

这是当前项目里非常值得重视的问题。

### 已确认的偏差类型

#### 1. 品牌不一致

当前仓库里同时存在：

- `Canopy`
- `GroveTab`
- `林栖标签页`
- 历史存储前缀 `canopy_*`

这本身不是问题，品牌迁移项目很常见。但问题是：

- `README.md` 和 `PRIVACY.md` 仍在写 `Canopy`
- 品牌配置层已经切到 `GroveTab`
- UI 文案又是 `林栖标签页`

对用户和评审方来说，这会造成“这是一个产品还是三个名字”的困惑。

#### 2. 文档提到的能力，不一定还在当前主界面上

`CHANGELOG`、`PRD`、旧报告多次强调：

- `DashboardOverview`
- `ActivityStrip`
- `HeroWidgets`
- `DailyQuote`
- `Weather`
- `Calendar`
- `Pomodoro`
- 自由 Widget 画布

但基于当前主入口 `App.tsx` 的实际渲染链路，我没有看到这些能力稳定地出现在当前首页主结构里。

更值得注意的是：

- `docs/PRD v1.0.md` 明确把 `Dashboard Overview` 放在 `HeroBar` 下方
- `CHANGELOG.md` 明确写了它已经上线
- `QA_CHECKLIST.md` 也把它列为通过项
- 但当前 `App.tsx` 并未渲染对应组件

同样，`ActivityStrip` 在文档中存在强叙事，但当前主界面链路中看不到稳定渲染入口。

#### 3. 部分样式和配置为未落地能力保留

例如当前样式里还能看到：

- `app-widget-card`
- `ActivityStrip` 相关样式注释
- `weather` 缓存配置
- `widgets` 文案

说明项目存在一部分“基础设施还在，但主路径不再挂载”的遗留状态。

### 这会带来什么体验问题

- 用户看到商店文案后，可能找不到对应功能
- 团队内部对“产品当前到底长什么样”会失去共识
- 后续每做一个改动，维护成本上升
- 体验评审会因为“承诺与现实不一致”而被扣分

### 结论

**当前项目的最大非功能性风险，不是功能缺少，而是“产品叙事版本分裂”。**

---

## 6.3 首次上手路径只覆盖了主工作台，没有覆盖产品全貌

当前 `OnboardingCard` 主要讲了三件事：

- 工作台总览
- 归档与恢复
- 快捷键帮助

这对于“标签工作台”本身是合理的，但问题在于，产品现在实际上还有：

- 热榜页
- 开发工具栏
- 深度外观定制
- Popup 入口
- 历史 / 书签授权型能力

也就是说，**引导讲的是核心主线，但产品入口已经不止主线**。

这会导致一种典型体验断层：

- 新用户被教育成“这是标签整理工具”
- 进入界面后却发现还有热榜和开发工具栏
- 这些入口没有在引导中被解释
- 于是产品会显得“突然很大”

这不是说要把所有能力都塞进 onboarding，而是要明确：

- 哪些是主线
- 哪些是附加空间
- 哪些是进阶功能

当前这三层没有被明确表达。

---

## 6.4 设置面板能力很强，但已经接近“过载”

设置系统是强项，同时也是当前体验负担最大的地方之一。

### 好处

- 可调能力极多
- 反映即时
- 对高级用户很友好
- 数据管理和危险操作都给了完整入口

### 代价

- 面板密度非常高
- 一般用户难以理解哪些设置值得改
- 视觉定制、搜索策略、权限开关、背景、动效、视频、危险区都集中在同一体系里
- 数据面板承担了太多职责：预设、导入导出、清空、重置、重播 onboarding、恢复出厂

换句话说，它已经不是“设置”，而更像“控制台”。

### 具体问题

#### 1. 渐进披露不够

现在很多高级能力直接裸露在设置中：

- 皮肤 token 精细定制
- 背景图压缩上传
- 视频背景
- 历史建议
- 热词来源
- 权限控制
- 工厂重置

这对高级用户是好事，但对普通用户是压迫感。

#### 2. 任务导向不够强

用户真正想做的是：

- 我想更简洁一点
- 我想更好看一点
- 我想搜索更强一点
- 我想恢复备份
- 我想清理数据

而不是：

- 我要设置 `borderWidth`
- 我要决定 `hotSuggestionSource`
- 我要处理 `videoBackground.fileKey`

当前面板更偏系统参数，而不是用户任务语言。

#### 3. 危险区太重

`DataPanel` 里把：

- 恢复默认配置
- 重播欢迎教程
- 恢复出厂设置

放在一个很直接的链路里，虽然有二次确认，但整体仍偏“工程控制面板”，而不是“日常用户设置”。

### 结论

**当前设置系统适合重度用户，不够适合普通用户。**

---

## 6.5 隐私叙事很强，但热榜能力与之存在张力

项目的隐私定位是强卖点，但 `TrendingPage` 当前依赖第三方接口：

- `https://api.xcvts.cn/api/hotlist`

这会形成一个产品层面的冲突：

- 工作台、归档、搜索、开发工具栏都在讲“本地优先”
- 热榜页天然要请求外部内容
- 用户容易把“热榜外部请求”误解为“产品本身在上传数据”

从技术上看，两者不是一回事；从用户感知上看，两者会混在一起。

### 这带来的问题

- 破坏产品的一致性叙事
- 增加隐私敏感用户的怀疑
- 第三方接口稳定性会直接影响新标签页观感
- 若热榜成为一级入口，外部依赖会被放大成主体验问题

### 更现实的问题

热榜页是信息消费场景，不是工作台刚需。一旦接口波动，用户第一印象会变差，但并不会让核心工作台更有价值。

### 结论

**热榜页不是不能做，而是不该与“本地优先工作台”站在同等品牌主叙事位置。**

---

## 6.6 存在“基础设施在，界面没挂上”的维护噪音

从当前仓库状态看，至少存在以下现象：

- 文档保留旧能力叙事
- 样式保留旧组件痕迹
- 存储 schema 已为部分高阶能力预留
- 文案系统里存在未完全挂载的产品模块

这种状态对开发团队很危险，因为它会导致：

- 评审时难以判断某功能是否真的在用
- 新成员容易误以为某能力已上线
- 后续重构时不敢删，也不敢全面用
- 产品越做越像“功能遗址博物馆”

这个问题暂时不会让用户立刻崩溃，但会持续侵蚀产品清晰度。

---

## 6.7 Popup 的定位很准，但与文档仍有轻微偏差

`CHANGELOG` 和 `QA_CHECKLIST` 中对 Popup 的描述偏“最近 10 个激活 Tab、360×520 四区”。

而当前代码里：

- `popup-shell` 实际尺寸是 **380×600**
- 列表展示的是**全部已打开 Tab**，只是按 `lastAccessed` 排序
- 不是严格“最近 10 个”

这不一定是坏事，实际上现在的体验可能更实用，但它再次说明：

> **产品说明与当前实现不同步。**

---

## 七、视觉体验评价

## 7.1 总体判断

如果单看视觉系统，这个项目明显高于大多数同类扩展。

原因不是用了好看的颜色，而是：

- 字体、圆角、模糊、阴影、层级都有系统化定义
- 不是单页拼出来的，而是整站可切换主题
- Popup、Trending、主界面都能共享同一设计语言

### 我的判断

- **设计系统能力**：强
- **最终界面一致性**：中上
- **视觉风格控制力**：强
- **产品聚焦度**：弱于视觉能力

也就是说：

> **视觉系统已经是平台级，产品结构还停留在功能集合级。**

---

## 7.2 视觉上的优点

### 1. 主题系统是真的可用，不是装饰品

九套皮肤不是简单换色，而是改变：

- 字体气质
- 控件密度
- 阴影和模糊强度
- Header 和搜索框风格
- 整体观感节奏

这让产品具备较强的审美适配能力。

### 2. Hero 搜索区是视觉焦点，符合主任务

首页把搜索放在 Hero 区中间，这是对的。因为对于标签管理产品来说，搜索本就是最高频动作之一。

### 3. Popup 视觉收敛得不错

Popup 没有为了美观牺牲信息密度，整体很克制。

### 4. Apple 皮肤并非表面模仿

从 `skin-presets.ts` 和样式覆盖来看，Apple 皮肤对字体、毛玻璃、搜索框、按钮圆角、阴影都做了系统化适配，而不是只改了蓝色。

---

## 7.3 视觉上的问题

### 1. 设计很强，但产品层次不总是跟上

由于入口过多，视觉焦点会被多条链路争用：

- Header 上有一级空间切换
- Hero 区有搜索
- 下面有整理建议
- 侧边有视图切换
- 再加设置、洞察、Popup、热榜、开发工具栏

结果是：界面本身不丑，但**产品注意力分配不够干净**。

### 2. 高级定制过强，普通用户可能没有必要理解这么多

从产品体验角度看，并不是所有可配置项都值得暴露给用户。当前视觉定制能力太强，反而弱化了“默认即好用”的产品自信。

### 3. 一些遗留样式会带来系统感噪音

例如样式中仍保留 `widget`、`ActivityStrip` 等痕迹，但当前主界面未必呈现对应能力。这会增加后续视觉维护难度。

---

## 八、交互体验评价

## 8.1 做得好的地方

### 1. 快捷键心智强

- 搜索有明确快捷键
- 多选有配套快捷键
- Onboarding 会介绍快捷键
- Popup 也支持回车直搜

这非常符合效率工具定位。

### 2. 撤销思路正确

Undo 系统对标签管理产品很重要，因为“误关”和“误归档”是高频焦虑点。当前项目在这件事上是有产品意识的。

### 3. 批量操作与整理建议有实际价值

很多同类产品只会“展示标签”，而不会给出整理动作。当前项目不仅展示，还试图推动用户完成整理，这是好方向。

### 4. 可选权限策略合理

- 历史权限按需申请
- 书签权限按需申请
- `<all_urls>` 只有用户显式开启 OG 抓取才申请

这类设计对用户信任非常有帮助。

---

## 8.2 需要改进的地方

### 1. 新标签页的一级切换太重

用户每次打开新标签页，最该看到的应该是主任务。现在把 `workspace / trending / devtools` 放在一级切换，会稀释主任务感。

### 2. 搜索足够强，但理解成本也高

搜索支持：

- 标签搜索
- 历史建议
- 热词建议
- 多搜索引擎
- 网页 fallback
- 拼音匹配

这在能力上非常强，但在体验上会让搜索从“一个命令面板”逐渐变成“一个多模态分发器”。如果没有更强的信息组织，用户会感到它很复杂。

### 3. DataPanel 交互像运维后台

对用户来说，“导出、导入、重置、工厂恢复”当然有价值，但这些都集中到一个面板后，会让设置系统显得过重。

### 4. 关于页是好的，但证明了主页面叙事还不够完整

当一个产品需要在关于页再解释一次“我有什么能力、怎么用”，往往意味着主页面本身还没有把核心价值讲清。

---

## 九、工程状态如何影响产品体验

## 9.1 正向影响

### 测试通过，说明核心功能可信度较高

- 169 个测试通过
- 说明纯逻辑层稳定
- 对标签管理、工具转换、搜索匹配等能力形成支撑

### 构建成功，说明产品仍处于可交付状态

- `pnpm build` 成功
- 新标签页和 Popup 产物均可正常生成

### 存储策略成熟，提升长期体验

- `chrome.storage.local` + `IndexedDB` 自动降级
- 视频背景单独走 IDB
- 归档可迁移
- 本地指标和缓存都有清理机制

这些都会减少“越用越坏”的风险。

---

## 9.2 负向影响

### 1. Lint 不能跑通，影响工程可信度

当前 `eslint.config.js` 依赖 `@eslint/js`，但 `package.json` 里未正确满足这条依赖，导致：

- 代码规范检查链路不可用
- 提交前静态保障下降
- 团队协作时更容易出现风格与质量漂移

这不一定会立刻影响用户，但它会影响产品长期稳定迭代。

### 2. 构建虽成功，但 chunk 分布暴露了结构问题

本次构建里可以看到几个明显偏大的块：

- `feat-insights`：约 **746 KB**
- `vendor-search`：约 **428 KB**
- `feat-settings`：约 **220 KB**

这说明当前项目虽然主入口体积控制得还可以，但一些次级功能的包体较重。对产品体验的影响是：

- 首屏问题不大
- 但一旦进入某些高级模块，加载成本会明显增大

尤其是 `Insights` 和 `Settings`，它们都不是低频到可以完全忽略体积的模块。

### 3. 文档与当前实现不同步，会放大协作成本

这会导致：

- 设计评审结论失真
- 开发估算失真
- 商店文案和真实产品不一致
- 版本节奏不清晰

这已经不是“文档问题”，而是“产品管理问题”。

---

## 十、当前项目的关键矛盾

我认为这个项目现在最核心的矛盾有三个。

### 矛盾一：主产品很清楚，一级入口却不够清楚

- 实际核心：标签工作台
- 界面表达：工作台 / 热榜 / 开发工具栏并列

### 矛盾二：设计系统很成熟，产品叙事却不够收束

- 主题、视觉、组件系统：成熟
- 用户一句话理解产品：不够稳定

### 矛盾三：工程基础很扎实，但产品版本面貌不够统一

- 构建、测试、存储、模块划分：较稳
- 品牌、文档、功能挂载状态：不够统一

这三个矛盾如果不解决，产品会越来越像“能力仓库”，而不是“一个被记住的产品”。

---

## 十一、产品评分

以下评分基于“当前仓库可确认状态”，满分 10 分。

| 维度 | 评分 | 说明 |
| --- | --- | --- |
| 产品定位清晰度 | 6.5 | 核心其实清楚，但一级入口过重，副业影响主业 |
| 核心价值强度 | 8.5 | 标签整理、搜索、归档、隐私都是真价值 |
| 新手上手体验 | 6.5 | onboarding 有，但无法覆盖当前产品全貌 |
| 重度用户效率 | 8.8 | 搜索、快捷键、视图、Popup、归档都很强 |
| 视觉设计成熟度 | 9.0 | 主题系统和皮肤体系明显高于常规扩展 |
| 交互一致性 | 7.2 | 核心区不错，但产品分支过多导致一致性打折 |
| 隐私与信任感 | 8.2 | 本地优先做得真，但热榜外部 API 拉低了一致性 |
| 工程可维护性 | 7.8 | 基础较强，但存在遗留能力与文档偏差 |
| 版本可信度 | 6.8 | 测试和构建好，lint 链路缺失，文档落后 |
| 综合建议分 | 7.9 | 是一个值得继续推进的产品，但现在最需要的是收束而不是继续加功能 |

---

## 十二、优先级建议

## P0：必须先处理

### 1. 重新定义产品主叙事

建议明确写成：

> `GroveTab 是一个本地优先的标签页工作台。`

然后再补一句：

> `同时提供热榜浏览与开发工具两个附加空间。`

不要反过来写成“全能新标签页”。

### 2. 清理品牌与文档不一致

至少统一：

- `README`
- `PRIVACY`
- `QA_CHECKLIST`
- `CHANGELOG`
- 商店文案源文件

让它们和当前 `GroveTab / 林栖标签页` 的真实状态一致。

### 3. 对齐“当前实现”和“当前承诺”

如果 `DashboardOverview`、`ActivityStrip`、`HeroWidgets` 确实已不在当前主链路：

- 要么恢复
- 要么删掉旧叙事
- 要么在 roadmap 中明确写成“暂时下线 / 重构中”

不要继续让文档和界面分裂。

---

## P1：下一轮应重点优化

### 4. 调整一级信息架构

建议把 `workspace` 进一步强化为默认主空间，降低 `trending` 和 `devtools` 的一级权重，例如：

- 工作台保留一级主入口
- 热榜和开发工具栏改成次级入口
- 或在 Header 中弱化展示方式

### 5. 重做设置的信息组织

建议按用户任务重组，而不是按技术属性平铺：

- **我想更简洁**
- **我想更好看**
- **我想搜索更快**
- **我想管理数据**
- **我想恢复默认**

而不是继续只按 Appearance / Behavior / Data 生硬切分。

### 6. 补一版“当前真实功能”的新手引导

尤其要告诉用户：

- 主空间是什么
- 热榜是什么
- 开发工具栏是什么
- Popup 和新标签页的关系是什么

---

## P2：中期优化

### 7. 决定热榜页的战略位置

二选一即可：

- 要么承认它是副功能，降低入口权重
- 要么把它做成真正独立、可信、可配置的信息产品

现在这种“存在感很强，但又不是主价值”的状态最尴尬。

### 8. 处理遗留 widget / dashboard 基础设施

对所有“样式还在、文档还在、主页面不一定还挂载”的模块做一次盘点：

- 真保留：补回主链路
- 不保留：删样式、删文案、删旧文档
- 未来再做：进 roadmap，不要伪装成已交付

### 9. 修复 lint 工具链

这件事不显眼，但必须做。因为产品下一阶段要解决的是“统一与收束”，而这需要高可信的工程护栏。

---

## 十三、最终结论

`GroveTab` 当前最强的地方，不是某一个单点功能，而是它已经形成了一个**可信的本地工作台底座**：搜索、归档、去重、权限、主题、Popup、数据管理，这些核心环节都不是玩具级实现。

它当前最需要解决的，也不是再加新能力，而是把以下三件事做干净：

- **主价值重新收束**
- **文档与现实重新对齐**
- **副功能与主功能重新分级**

如果这三件事做好，我认为这个项目会从“功能很多的扩展”提升为“有清晰人格的产品”。

如果这三件事继续拖着不处理，项目会越来越难讲清，也越来越难维护。

---

## 十四、给团队的一句建议

> **下一版不要优先问“还能加什么”，而要先问“用户第一次打开时，最应该看到什么”。**

这会决定 `GroveTab` 是变成一个越来越强的产品，还是变成一个越来越满的产品。
